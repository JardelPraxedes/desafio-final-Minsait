//   beforeEach('visitando página opensource', ()=>{
//       cy.visit('https://opensource-demo.orangehrmlive.com/')
//   })

//   it('verificando visibilidade dos elementos da tela de login', ()=>{
//     cy.get('[alt="company-branding"]').should('be.visible') //verificando visibilidade logo header
//     cy.get('[alt="orangehrm-logo"]').should('be.visible') //verificando visibilidade logo
//     cy.get('[name="username"]').should('be.visible') //verificando visibilidade campo username
//     cy.get('[placeholder="Password"]').should('be.visible') //verufucabdi visibilidade campo password
//     cy.get('[class="oxd-button oxd-button--medium oxd-button--main orangehrm-login-button"]').should('be.visible') //verificando visibilidade botão
// })
  
// it('logando errado', ()=>{
//   cy.get('[name="username"]').type('Admin123')
//   cy.get('[placeholder="Password"]').type('admin111')
//   cy.get('[class="oxd-button oxd-button--medium oxd-button--main orangehrm-login-button"]').click()
  
// })

//   it('logando na página', ()=>{
//       cy.get('[name="username"]').type('Admin')
//       cy.get('[placeholder="Password"]').type('admin123')
//       cy.get('[class="oxd-button oxd-button--medium oxd-button--main orangehrm-login-button"]').click()
      
//   })